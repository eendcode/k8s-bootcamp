# fundamentals/manifests/pod.yaml
kind: Pod
apiVersion: v1
metadata:
  name: nginx
  namespace: exercise
spec:
  containers:
    - name: nginx
      image: localhost:5000/kubectl
      ports:
        - containerPort: 80
